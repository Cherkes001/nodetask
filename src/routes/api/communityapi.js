const router = require('express').Router();
const communityController = require('../../controllers/db-controller.js');
const communitycontroller = new communityController();

router.get();

module.exports = router;
